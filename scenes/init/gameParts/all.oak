ml := import('ml')
weekmaster := import('weekmaster')
arrows := import('arrows')
parallax := import('parallax')

window.GAMEPARTS := {
  ml: ml
  weekmaster: weekmaster
  arrows: arrows
  parallax: parallax
}